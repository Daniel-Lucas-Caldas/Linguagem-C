#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <locale.h>

int main() {
    setlocale(LC_ALL, "portuquese");
    int maça;
    char resultado [250]; //String

    // Requisitando dados do usúario
    printf("Informe a quantidade de maças: ");
    scanf("%d", &maça);

  //Especificaçoes 
  Valor = 
  
  valortotal = maça
  maça = 1,30

  if (maça >= 12) {
    printf ("Total a pagar: ", valortotal * )
  }
  
  >= 65 || tempot >= 20) {
        strcpy(resultado,"Requerer aposentadoria!"); 
        } else {
        strcpy(resultado,"Não requerer aposentadoria!");
        }
