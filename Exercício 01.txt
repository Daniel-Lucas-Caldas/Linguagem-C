#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <locale.h>

int main () {
  setlocale(LC_ALL, "portuguese");
  int temperatura;

 printf("Por favor digite a temperatura ambiente: ");
  scanf("%d", &temperatura);

if (temperatura >= 25) {
  printf("o clima está ensolarado");
} else if (temperatura <= 15) {
  printf("o clima está chuvoso");
} else if (temperatura >= 15 && temperatura <=25) {
  printf("o clima está nublado");
}
  return 0;
}
